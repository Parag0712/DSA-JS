// O(1) aka constant time, signifies that the execution time for of algorithm remains
// constant regardless of the input size
const numbers = [1, 2, 3, 4, 5];
const getElement = (arr, index) => arr[index];
console.log(getElement(numbers, 0));